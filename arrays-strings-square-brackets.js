const mySentence = ['hey', 'here', 'is', 'a', 'sentence', 'in', 'parts'];
// (console.log will evaluate expressions to strings)
// console.log(mySentence[4]);

// strings have some similarities with arrays
// both can access elements by index
// some of their built-in methods are different

const myColor = 'purple';

console.log(myColor);

// two different ways to access the element at index 2 of a string
console.log(myColor[2]);
console.log(myColor.charAt(2));